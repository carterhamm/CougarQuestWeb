rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // Allow anyone (authenticated or not) to read or write any document.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}